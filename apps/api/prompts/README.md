# Prompt templates

Versioned LLM prompt templates live here (never inline strings in code).
Naming: `<feature>/<name>.v<N>.md`, e.g. `listing_builder/describe_ar.v1.md`.
Bump the version instead of editing a template that has shipped; the version is logged with every LLM call.
