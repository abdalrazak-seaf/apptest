# Fair-Price Engine (Phase 3)

Placeholder. Phase 3 adds: the CSV import tool (`make import-price-data FILE=...`) with the column
spec documented here, LightGBM quantile models (p10/p50/p90), k-nearest-comparables fallback,
versioned artifacts in `models/`, and an evaluation script (MAE + interval coverage by segment).
